#===================================================================================#
#_______________________----------- Lumen Summary -----------_______________________#
#===================================================================================#

RAEDME {
	"type": text file;
	"goal": provides a quick and painless guide about the tool usage;
	"dependencies": N/A;
	"details": works by.... existing i guess ^_^
},

auth.sh {
	"type": bash script;
	"goal": provide a GUI for authentication;
	"dependencies": Zenity (https://gitlab.gnome.org/GNOME/zenity); 	
	"details": works by taking the user input and matching it with the database.
},

setup.sh {
	"type": bash script;
	"goal": automatically install and manage Lumen on a fresh system;
	"dependencies": N/A;
	"details": works by creating the directory, files and setting access control.
},

users.csv {
	"type": Comma-Separated Values;
	"goal": store users login data;
	"dependencies": N/A;
	"details": works by providing the database for auth.sh to lookup.
},

users(date).csv {
	"type": Comma-Separated Values;
	"goal": store authenticated users;
	"dependencies": N/A;
	"details": works by taking the valid output of auth.sh and storing on itself with the timestamp.
},

/srv/lumen {
	"type": directory;
	"goal": be a unified place of the tool;
	"dependencies": N/A;
	"details": works by storing everything in one place to keep it all tidy up.	
},

/srv/lumen/db {
	"type": directory;
	"goal": keep the database safe;
	"dependencies" N/A;
	"details": works by storing the users.csv and keeping it read-only from non-superusers.
},

/srv/lumen/backups {
	"type": directory;
	"goal": keep the authenticated users safe;
	"dependencies": N/A;
	"details": works by keeping a copy of everyday auth safe and locked away(append-only).	
},

Lumen {
	"type": user;
	"goal": security lock for the tool;
	"dependencies": N/A;
	"details": be the dedicated user and owner of the tool, with no way to login.
},

lumain {
	"type": group;
	"goal": same as the user Lumen;
	"dependencies": N/A;
	"details": be the dedicated group of the admin user(s).
},

lumuser {
	"type": group;
	"goal": provide access to the auth;
	"dependencies": N/A;
	"details": works by allowing user(s) only from this group to authenticate.
}

lumen-logs.service {
	"type": systemd file;
	"goal": automatically create backup files;
	"dependencies": systemd init system;
	"details": works by securing a users(date).csv with append-only property.
},
lumen-logs.timer {
	"type": systemd file;
	"goal": run the .service file;
	"dependencies": systemd init system;
	"details": works by running the .service everyday at 2AM or whenever the machine boots.
}
